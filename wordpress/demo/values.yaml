# wordpress/demo/values.yaml
wordpressUsername: admin
wordpressPassword: "JW@PUvdgiL8z95GENp*dETUH"

service:
  type: ClusterIP

ingress:
  enabled: true
  hostname: "demo.kubeoptim.com"
  ingressClassName: traefik
  tls: false
  annotations: {}

replicaCount: 1
resources:
  requests: { cpu: 50m,  memory: 128Mi }
  limits:   { cpu: 200m, memory: 256Mi }

volumePermissions:
  enabled: true

persistence:
  enabled: true
  storageClass: longhorn-1r
  size: 2Gi

mariadb:
  enabled: true
  architecture: standalone
  auth:
    database: "bitnami_wordpress"
    username: "bn_wordpress"
    password: "x6*9wDccimpw!u96CeXJe6nr"
    rootPassword: "wCw!hpDt%X8yyZZr%S!XQNCs"
  primary:
    persistence:
      enabled: true
      storageClass: longhorn-1r
      size: 2Gi
    resources:
      requests: { cpu: 50m,  memory: 128Mi }
      limits:   { cpu: 200m, memory: 256Mi }
  metrics:
    enabled: false
  volumePermissions:
    enabled: true

allowEmptyPassword: false
extraEnvVars:
  - name: ALLOW_EMPTY_PASSWORD
    value: "no"

metrics:
  enabled: false
